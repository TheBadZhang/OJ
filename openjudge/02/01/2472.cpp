#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;
struct S {
	string str;
	int times;
}
vector <S> list;


int main () {

	string str;
	cin >> str;
	s = str.size ();
	for (int a = 0; a < s; a += 1) {
		for (int b = a; b < s; b += 1) {
			
		}
	}

	sort (list.begin (), list.end (), [](S a, S b) -> bool {return a.str > b.str;});




	return 0;
}
#include <iostream>

using namespace std;

int main () {

	int T;
	cin >> T;
	while (T--) {
		int a [100100] = {0};
		int b [100100] = {0};
		int pay = 0;
		int N;
		cin >> N;
		for (int i = 0; i < N; i++) {
			scanf("%d", &a[i]);
		}
		for (int i = 0; i < N; i++) {
			scanf("%d", b[i]);
		}
	}





	return 0;
}